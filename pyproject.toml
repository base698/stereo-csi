[project]
name = "stereo-csi"
version = "0.1.0"
description = "Build your own CSI stereo depth camera on a Jetson: capture, calibration, and depth"
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "opencv-python",
]

[tool.setuptools]
packages = ["stereo_csi"]
